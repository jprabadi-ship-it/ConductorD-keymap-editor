<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
  <dict>
    <!-- Electron baseline (matches electron-builder's defaults) -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    <!-- Hardened Runtime blocks CoreBluetooth without this, which made
         Web Bluetooth's requestDevice reject instantly with
         "adapter not available" (Connect BLE silently did nothing). -->
    <key>com.apple.security.device.bluetooth</key>
    <true/>
    <!-- Web Serial device access under Hardened Runtime. -->
    <key>com.apple.security.device.usb</key>
    <true/>
  </dict>
</plist>
